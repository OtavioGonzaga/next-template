export type Translations = {
	dashboard: string;
	customers: string;
	settings: string;

	errors: {
		notFound: string;
	};

	actions: {
		delete: string;
		edit: string;
		save: string;
		cancel: string;
		create: string;
		update: string;
		reset: string;
		return: string;
		returnTo: string;
		logout: string;
	};

	fields: {
		selectEntity: string;
	};
};
